<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="NetworkMonitor.Views.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:NetworkMonitor.Views.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid
        RowDefinitions="Auto,*">

        <SelectorBar
            Grid.Row="0"
            x:Name="TabBar"
            FontSize="13"
            Margin="24,12,24,0"
            SelectionChanged="TabBarSelectionChanged">

            <SelectorBarItem
                Tag="Digest"
                Text="Daily Digest" />

            <SelectorBarItem
                Tag="History"
                Text="History" />

        </SelectorBar>

        <Grid
            Grid.Row="1"
            x:Name="DigestPanel"
            RowDefinitions="Auto,*">

            <StackPanel
                Grid.Row="0"
                Margin="16,8,16,0"
                Orientation="Horizontal"
                Spacing="8">

                <Button
                    Command="{x:Bind ViewModel.GenerateNowCommand}">

                    <StackPanel
                        Orientation="Horizontal"
                        Spacing="8">

                        <ProgressRing
                            Width="16"
                            Height="16"
                            IsActive="{x:Bind ViewModel.GenerateNowCommand.IsRunning, Mode=OneWay}"
                            Visibility="{x:Bind RunningToVisibility(ViewModel.GenerateNowCommand.IsRunning), Mode=OneWay}" />

                        <TextBlock
                            VerticalAlignment="Center"
                            Text="{x:Bind GenerateButtonLabel(ViewModel.GenerateNowCommand.IsRunning), Mode=OneWay}" />

                    </StackPanel>

                </Button>

                <Button
                    Content="Export PDF"
                    Click="ExportDigestPdfClick" />

            </StackPanel>

            <ScrollViewer
                Grid.Row="1"
                Padding="16">

                <controls:DigestReportView
                    Summary="{x:Bind ViewModel.LatestSummary, Mode=OneWay}"
                    Report="{x:Bind ViewModel.LatestReport, Mode=OneWay}" />

            </ScrollViewer>

        </Grid>

        <Grid
            Grid.Row="1"
            x:Name="HistoryPanel"
            ColumnDefinitions="280,*"
            Visibility="Collapsed">

            <Grid
                Grid.Column="0"
                RowDefinitions="*,Auto">

                <ListView
                    Grid.Row="0"
                    x:Name="HistoryList"
                    ItemsSource="{x:Bind ViewModel.Reports, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedHistoryReport, Mode=TwoWay}">

                    <ListView.ItemTemplate>

                        <DataTemplate
                            xmlns:models="using:NetworkMonitor.Models.Digest"
                            x:DataType="models:DigestReport">

                            <StackPanel
                                Padding="4"
                                Spacing="2">

                                <TextBlock
                                    FontWeight="SemiBold"
                                    Text="{x:Bind PeriodEndDisplay}" />

                            </StackPanel>

                        </DataTemplate>

                    </ListView.ItemTemplate>

                </ListView>

                <Button
                    Grid.Row="1"
                    Margin="8"
                    HorizontalAlignment="Stretch"
                    Content="Export All Reports To CSV"
                    Click="ExportAllReportsCsvClick" />

            </Grid>

            <Grid
                Grid.Column="1"
                RowDefinitions="Auto,*">

                <StackPanel
                    Grid.Row="0"
                    Margin="16,8,16,0"
                    Orientation="Horizontal"
                    Spacing="8">

                    <Button
                        Content="Export PDF"
                        Click="ExportHistoryPdfClick" />

                    <Button
                        Content="Delete"
                        Click="DeleteHistoryClick" />

                </StackPanel>

                <ScrollViewer
                    Grid.Row="1"
                    Padding="16">

                    <controls:DigestReportView
                        Summary="{x:Bind ViewModel.SelectedHistorySummary, Mode=OneWay}"
                        Report="{x:Bind ViewModel.SelectedHistoryReport, Mode=OneWay}" />

                </ScrollViewer>

            </Grid>

        </Grid>

    </Grid>

</Page>
