namespace NetworkMonitor.UITests.Driving
{
    // Empty until the FlaUI driving layer (Application plus main and mini-graph windows) is
    // built; it exists now so PhaseContext.Session has a concrete type to hold and swap.
    public sealed class AppSession
    {
    }
}
